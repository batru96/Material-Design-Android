package com.example.batru.materialdesign.models

class Infomation(private var iconId: Int, private var title: String) {

    fun getIconId(): Int = this.iconId
    fun getTitle(): String = this.title
}